
1.PrintJob weiter optimieren

2.Tabellen automatisch auf Aenderungen reagieren lassen (TableModelListener)

3.MigLayout testen fuer den GUI Builder

Fehler beheben:
 	MyWindowAdapter: this.frame.openClosingDialog("CloseWindow");
	Unterstuetzt nun nur noch englisch.. entsprechend in der MainAnwendung anpassen !
	Same for pathChooser with Directory
	
4. Print und E-Mail thread basierte Exceptions abfangen

5. Print abort Exception behandeln