1.PrintJob code / performance improvements

2.Auto refresh tables after changes (TableModelListener)

3.MigLayout test for GUIBuilder

Errors to fix:
 	MyWindowAdapter: this.frame.openClosingDialog("CloseWindow");
	Sadly only English supported right now.. to fix in the main application !
	Same for pathChooser with Directory
	
4. Catch print and eMail thread based exceptions

5. Take care of print abort Exception

6. Develop JavaFX GUIBuilder

7. Implement new Java 8 compatible DateHelper (DateHelperNew) (toRename after implementation)
